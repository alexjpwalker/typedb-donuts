// Game configuration constants

/** Starting balance for each retail outlet */
export const OUTLET_STARTING_BALANCE = 1000;

/** Factory auto-regulation: pause production when active orders exceed this */
export const FACTORY_PAUSE_THRESHOLD = 20;

/** Factory auto-regulation: resume production when active orders fall to this or below */
export const FACTORY_RESUME_THRESHOLD = 10;
